.venv/
__pycache__/
*.pyc
.env
# A YouTube login in a file. Never committed, for the same reason .env isn't.
cookies.txt
yt-cookies-*.txt
*.egg-info/
build/
dist/
.pytest_cache/

# Runtime output
out/
state/ledger.json
# Settings RYTE changes about itself. Untracked on purpose: a local edit to a
# tracked file blocks the auto-update from fast-forwarding.
state/preferences.json
